{
  "name": "@hypabolic/trajectory-workspace",
  "private": true,
  "version": "0.1.1",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build -w @hypabolic/trajectory && npm run build -w @hypabolic/trajectory-node && npm run build -w @hypabolic/trajectory-otel && npm run build -w @hypabolic/trajectory-testing && npm run build -w @hypabolic/trajectory-cli",
    "clean": "npm run clean --workspaces --if-present",
    "test": "npm run build && node --test test/*.test.mjs",
    "typecheck": "npm run build && npm run typecheck --workspaces --if-present",
    "benchmark": "npm run build && node benchmarks/benchmark.mjs"
  },
  "devDependencies": {
    "@types/node": "22.20.1",
    "typescript": "7.0.2"
  }
}
